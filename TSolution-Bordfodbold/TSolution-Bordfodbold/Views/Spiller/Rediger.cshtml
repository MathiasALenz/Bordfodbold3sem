@model TSolution_Bordfodbold.Entities.Spiller

@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Rediger @Model.Navn</title>
</head>
<body style="background-color: lightgray">
    @using (Html.BeginForm("Rediger", "Spiller"))
    {
        <div class="panel">
            <div class="panel-body">
                @Html.HiddenFor(s => s.Spiller_ID)
                <table class="table table-condensed table-bordered">
                    @foreach (var property in ViewData.ModelMetadata.Properties)
                    {
                        switch (property.PropertyName)
                        {
                            case "Spiller_ID":
                            case "Scorede_Maal":
                            case "Indkasserede_Maal":
                            case "Vundne":
                            case "Uafgjorte":
                            case "Tabte":
                            case "WS": break;
                            case "Administrator":
                                <tr>
                                    <div class="form-group">
                                        <td>
                                            <b>@(property.DisplayName ?? property.PropertyName)</b>
                                        </td>
                                        <td>
                                            @Html.CheckBox(property.PropertyName, false, new { @class = "form-control" })
                                        </td>
                                        @Html.ValidationMessage(property.PropertyName)
                                    </div>
                                </tr> break;
                            default:
                                <tr>
                                    <div class="form-group">
                                        <td>
                                            <b>@(property.DisplayName ?? property.PropertyName)</b>
                                        </td>
                                        <td>
                                            @Html.TextBox(property.PropertyName, null, new { @class = "form-control", rows = 1 })
                                        </td>
                                        @Html.ValidationMessage(property.PropertyName)
                                    </div>
                                </tr>break;
                        }
                    }
                </table>
            </div>
            <div class="panel-footer">
                <input type="submit" value="Gem" class="btn btn-primary" />
                @Html.ActionLink("Afslut og vend tilbage til listen", "Spillere", null, new { @class = "btn btn-default" })
            </div>
        </div>

    }
</body>
</html>